@model LuyenThiTracNghiem.Areas.Admin.Models.PagedResult<LuyenThiTracNghiem.Areas.Admin.Models.tblExam>

<div class="table-responsive">
    <table class="table table-bordered table-hover">
        <thead>
            <tr>
                <th>ID</th>
                <th>Exam Name</th>
                <th>Question Count</th>
                <th>Duration Minutes</th>
                <th>Exam Type</th>
                <th>Status</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        @if (Model.Items.Any())
        {
            foreach (var item in Model.Items)
            {
                <tr>
                    <td>@item.ExamId</td>
                    <td class="fw-semibold">@item.ExamName</td>
                    <td>@item.QuestionCount</td>
                    <td>
                        @item.DurationMinutes
                    </td>
                    <td>@item.ExamType</td>
                    <td>
                        <span class="status-badge @(item.Status ? "status-active" : "status-inactive")">
                            @(item.Status ? "Active" : "Inactive")
                        </span>
                    </td>
                    <td>
                        <div class="d-flex gap-2">
                            <a class="btn btn-warning btn-sm" asp-area="Admin" asp-controller="Exam" asp-action="Edit" asp-route-ExamId="@item.ExamId">
                                <i class="fas fa-edit me-1"></i>Edit
                            </a>
                            <a class="btn btn-danger btn-sm" asp-area="Admin" asp-controller="Exam" asp-action="Delete" asp-route-ExamId="@item.ExamId">
                                <i class="fas fa-trash me-1"></i>Delete
                            </a>
                        </div>
                    </td>
                </tr>
            }
        }
        else
        {
            <tr>
                <td colspan="6" class="text-center text-muted">No exams found</td>
            </tr>
        }
        </tbody>
    </table>
</div>

<!-- Pagination -->
@if (Model.TotalPages > 1)
{
    <nav aria-label="Page navigation">
        <ul class="pagination">
            <li class="page-item @(Model.PageNumber == 1 ? "disabled" : "")">
                <a class="page-link" href="#" data-page="@(Model.PageNumber - 1)">Previous</a>
            </li>

            @for (int i = 1; i <= Model.TotalPages; i++)
            {
                <li class="page-item @(i == Model.PageNumber ? "active" : "")">
                    <a class="page-link" href="#" data-page="@i">@i</a>
                </li>
            }

            <li class="page-item @(Model.PageNumber == Model.TotalPages ? "disabled" : "")">
                <a class="page-link" href="#" data-page="@(Model.PageNumber + 1)">Next</a>
            </li>
        </ul>
    </nav>
}
